function letterCount(x)
{
    let dialogue = x;
    let count = dialogue.length;
    return(count);
}
console.log(letterCount("Hello"))
console.log(letterCount("Believe in yourself"));
console.log(letterCount("Be kind to all the lifes in world"));

//the .length method counts the no of values in a string including the spaces.